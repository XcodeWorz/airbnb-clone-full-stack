# import AuthResponse from "./modules/user/authResponse.graphql"

type Error {
  path: String
  message: String
}

type Query {
  test: String!
}

type Mutation {
  register(
    email: String!
    firstName: String!
    lastName: String!
    password: String!
    confirmPassword: String!
  ): AuthResponse!
}
